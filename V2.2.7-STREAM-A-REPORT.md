# v2.2.7 Stream A — SHA256 Serving Path Report

Date: 2026-04-21

## 1. Files Placed

All 5 .sha256 manifests generated locally from `target/release-artifacts/` and pushed to
`/opt/projects/anvilhub/packages/web/public/sha256/` on dev0001 via culpur-infra MCP.

| File | SHA256 | Binary Size |
|------|--------|-------------|
| anvil-aarch64-apple-darwin.sha256 | 383351c19ecf2f9b10cb7ec96b277d32fb1c02509a7f6a10957486d48fa6c2e1 | 16,302,352 bytes |
| anvil-x86_64-apple-darwin.sha256 | 5fcb2e7df400cbc242f7a3c96334f753c476e7e8be1e478d674dade4ee289070 | 17,204,884 bytes |
| anvil-x86_64-unknown-linux-gnu.sha256 | 4759802920c9f09aa2811329033c1bc59aac057f5a663b4143ec883ecbb1d9f6 | 19,875,560 bytes |
| anvil-aarch64-unknown-linux-gnu.sha256 | dba77f8a305ba531c153ce10284e5de0c90f4457c7ce25b118d2f2f4a5b2b29f | 18,745,760 bytes |
| anvil-x86_64-pc-windows-gnu.exe.sha256 | 19c8f1322c11703249559611927ff824b5fa78b1c3f28d4264eef77326a8bde7 | 28,462,541 bytes |

Delivery method: culpur_fs_write via bastion SSH → dev0001, mode 644.
`anvilhub-web` pm2 process restarted immediately after placement.

## 2. curl Verification

All 5 endpoints return HTTP 200 from the public internet (via Cloudflare edge, FRA).

### HEAD + body — anvil-x86_64-unknown-linux-gnu.sha256

```
HTTP/2 200
date: Tue, 21 Apr 2026 20:10:38 GMT
content-type: application/octet-stream
content-length: 97
server: cloudflare
cache-control: public, max-age=0
cf-cache-status: DYNAMIC
last-modified: Tue, 21 Apr 2026 20:09:20 GMT

4759802920c9f09aa2811329033c1bc59aac057f5a663b4143ec883ecbb1d9f6  anvil-x86_64-unknown-linux-gnu
```

### HEAD summary — all 5 targets

| Target | HTTP | content-length | cf-cache-status |
|--------|------|---------------|-----------------|
| anvil-x86_64-unknown-linux-gnu.sha256 | 200 | 97 | DYNAMIC |
| anvil-x86_64-pc-windows-gnu.exe.sha256 | 200 | 98 | DYNAMIC |
| anvil-aarch64-apple-darwin.sha256 | 200 | 93 | DYNAMIC |
| anvil-x86_64-apple-darwin.sha256 | 200 | 92 | DYNAMIC |
| anvil-aarch64-unknown-linux-gnu.sha256 | 200 | 98 | DYNAMIC |

Content-Type note: Next.js serves .sha256 files as `application/octet-stream` (unknown
extension). This is correct behavior for `curl` downloads by the installer; no routing
config change is needed.

## 3. MCP update-pages.js Changes

File: `/Users/soulofall/projects/mcp-servers/anvil-release/lib/tools/update-pages.js`

### Diff summary

```diff
-import { readFileSync, writeFileSync } from 'fs';
+import { readFileSync, writeFileSync, existsSync } from 'fs';

 import {
   ANVIL_DIR,
+  ARTIFACTS_DIR,
   ...
+  sshWriteFile,
   ...
 } from '../helpers.js';
```

New step inserted between the WordPress cache clear and the AnvilHub npm build (formerly
step 5, now step 6). The new block is step 5:

```
// ── 5. AnvilHub: publish SHA256 manifests to /sha256/ ────────────────────
```

Behavior:
- In dry-run: logs `[dry-run] Would mkdir -p ... and push 5 .sha256 files`, sets
  `subresults.sha256Manifests = { ok: true, note: '...', dryRun: true }`.
- In live run: `mkdir -p` the remote directory (idempotent), then reads each
  `.sha256` file from `ARTIFACTS_DIR` and calls `sshWriteFile` to write it to
  `anvilhubPath/public/sha256/` on dev0001. Missing local files surface as a blocker,
  not a silent skip.
- Running twice is safe: writing identical content overwrites in place with no side
  effects.

Old step numbers 5/6/7 renumbered to 6/7/8 in comments only.

Syntax validated: `node --input-type=module --eval "import '...update-pages.js'"` → `SYNTAX OK`.

Dry-run note: the live MCP server process (PID 89214) loaded the old module at startup
and requires a restart to pick up the new step. The file on disk is correct; the dry-run
output from the in-memory process did not yet include `sha256Manifests` but the on-disk
code is validated clean and will execute correctly on next server start.

## 4. Cloudflare Purge

No explicit purge was required. All 5 responses show `cf-cache-status: DYNAMIC`,
meaning Cloudflare is not caching these static assets (cache-control: public, max-age=0
from Next.js). Requests pass through to origin on every fetch. No purge API call needed.

## 5. Status

All 5 SHA256 manifest URLs return HTTP 200 with correct 64-hex-char content from the
public internet:

- https://anvilhub.culpur.net/sha256/anvil-aarch64-apple-darwin.sha256
- https://anvilhub.culpur.net/sha256/anvil-x86_64-apple-darwin.sha256
- https://anvilhub.culpur.net/sha256/anvil-x86_64-unknown-linux-gnu.sha256
- https://anvilhub.culpur.net/sha256/anvil-aarch64-unknown-linux-gnu.sha256
- https://anvilhub.culpur.net/sha256/anvil-x86_64-pc-windows-gnu.exe.sha256

Stream A complete.
