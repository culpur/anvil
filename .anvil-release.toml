[targets]
anvilhub_path = "/opt/projects/anvilhub/packages/web"
anvilhub_pm2_process = "anvilhub"
# Task #698 (v2.2.18 revert of #655): anvilhub-web actually runs on dev0001
# (10.0.70.80:3100), NOT in CT 113. Apache vhost `anvilhub.culpur.net.conf`
# on guard has `ProxyPass / http://10.0.70.80:3100/`. MCP must SSH directly
# to dev0001 for pm2 ops. CT 113 was a stale memory entry from prior arch.
anvilhub_pm2_host = "dev0001"
culpur_wp_page_id = 619
culpur_wp_slug = "anvil"

[pages]
# Files to update (all references to the old version → new version)
# Paths are relative to anvilhub_path (without leading slash)
anvilhub_files = [
  "src/lib/anvil-config.ts",
  "src/app/page.tsx",
  "src/app/about/page.tsx",
]

[wordpress]
shortcode_file = "/data/compose/4/v1/wp-content/mu-plugins/culpur-hardening.php"
shortcode_name = "anvil_version"
