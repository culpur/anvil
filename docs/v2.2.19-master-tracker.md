# Anvil v2.2.19 — Master Tracker

**Hero theme:** i18n + /ssh in the Web Viewer
**Tagline:** "Anvil speaks your language, anywhere."

Tracker for task #704. Living document — update as items land.

---

## Hero arcs (must-ship for v2.2.19)

### Arc A — i18n end-to-end (task #645)

8 Tier-1 locales (en + es + zh-CN + fr + pt-BR + ru + ja + de) wired through both the Anvil TUI and the AnvilHub web viewer, with a wizard picker, `/language` slash command, `--lang` CLI flag, browser auto-detect, and a viewer locale dropdown.

| Phase | Description | Status | Commit |
|-------|-------------|--------|--------|
| A1 | rust-i18n confirmed wired + pt-BR stub created + YAML wrap-bug fix | ✅ | `23234d5` |
| A2 | Extract wizard strings (175 calls) | ✅ | `23234d5` |
| A3 | Extract TUI mod.rs + cmd_static + cmd_ai (34 calls) | ✅ | `23234d5` |
| A4 | Seed 7 Tier-1 translations (260 keys × 7 = 1820 strings) | ✅ | `854a23c` |
| **A5** | `--lang` CLI flag + boot-time set_locale + pt-BR in SUPPORTED + drift gate | 🔄 | (in flight, agent `aaea3ce`) |
| **A6** | Wizard language picker + Configure menu picker | 🔄 | (in flight, agent `aaea3ce`) |
| W1 | viewer.html `t()` helper + sidecar JS | 🔄 | (in flight, agent `afced28`) |
| W2 | viewer.html string extraction (~140 literals) | 🔄 | (in flight, agent `afced28`) |
| W3 | Browser-language auto-detect | 🔄 | (in flight, agent `afced28`) |
| W4 | Locale dropdown in status bar | 🔄 | (in flight, agent `afced28`) |
| W5 | viewer.html 7 Tier-1 translations | 🔄 | (in flight, agent `afced28`) |

**Acceptance**: `/language es` (Anvil) + clicking "Español" in viewer dropdown → both surfaces re-render in Spanish. `anvil --lang fr` works for one-shot invocations. Wizard picker default = persisted locale. All 8 native-name labels render correctly (Chinese + Japanese + Cyrillic non-ASCII shapes).

### Arc B — /ssh in the Web Viewer (task #706 — DONE)

| Phase | Description | Status | Commit |
|-------|-------------|--------|--------|
| Audit | TUI /ssh + relay protocol + design spec (3 docs) | ✅ | docs untracked |
| Phase 1 | Backend: 11 relay variants + rate limit + vault gate + zeroize | ✅ | `75f32ec` |
| Phase 2 | viewer.html UI: xterm.js + form modal + host picker + key picker + terminal pane + status footer | ✅ | `aaf2fed` (passage) |
| Phase 3 | Integration tests: 6 tests + mock russh server, 1078/1078 lib tests pass | ✅ | `214f493` |

**Acceptance**: type `/ssh` in viewer → form modal opens → fill or pick saved host → terminal pane mounts with xterm.js → bidirectional bytes round-trip via relay → `/ssh close` or Disconnect button tears down cleanly.

### Arc C — Release Pipeline Hardening (carry-over from v2.2.18 post-mortem)

Three v2.2.18 release-day surprises that need structural fixes for v2.2.19+:

| Item | Description | Status |
|------|-------------|--------|
| C1 | release.sh per-step ID + explicit OK/FAIL gate (replaces `set -e` + `||`) — the v2.2.18 silent exit + the i18n-implementer's dirty-tree discovery both prove the `||`-only pattern is insufficient | ⏳ pending |
| C2 | /about page → ISR `revalidate=60` + on-deploy webhook (replaces force-dynamic + 1hr Cloudflare stale window) | ⏳ pending |
| C3 | Puppet exec post-receive hook on /opt/passage to enforce 644 after every git pull (v2.2.18 day-of perm regression despite #691) | ⏳ pending |

**Acceptance**: next release runs cleanly through every phase without silent exits; /about reflects new versions within 60s of deploy; passage git pulls never leave files at 640 again.

---

## Stretch arcs (v2.2.19 if time, otherwise v2.2.20)

### Arc D — AnvilHub Build page (#674 + #675 prerequisite)

Pairs with v2.2.18's in-TUI /mcp builder. A web micro-service at anvilhub.culpur.net/build that lets browser users generate MCP servers via the same AI-assisted spec flow.

| Item | Description |
|------|-------------|
| #675 (security) | anvil-mcp-builder operator OAuth token in vault, not .env (BLOCKER for #674) |
| #674 | anvil-mcp-builder micro-service + Build page |

**Acceptance**: navigate to /build, fill the spec form, get a downloadable tarball of a working MCP server.

### Arc E — CC parity sweep v2.1.144+

Last audit was v2.1.143 on May 17, 2026. ~4 days of CC drift to catch up on. Standard pattern: audit → file per-feature tasks → triage v2.2.19 vs v2.2.20 → land the v2.2.19 items.

### Arc F — Twitter post-pipeline automation (#659 follow-on)

v2.2.18 X announcements landed via a one-shot script that read scanner-LXC `.env` creds. v2.2.19 should:
- Vault the 8 X_* credentials properly (#709)
- Wire release.sh Phase 7 to actually post automatically when `RELEASE_AUTO_POST=1` (currently print-only)
- PKCE enrollment script `enroll-x-pkce.js` confirmed working end-to-end

---

## Fleet/ops carry-overs (NOT v2.2.19-scoped, listed for awareness)

These are pending but not blocking the release. Triage in their own threads.

- #525 — P0 fleet-wide LAN connectivity incident (still pending; may auto-resolve)
- #161 — Apache file capture into profile::apache_edge/files
- #162 — Merge feat/profiles-roles-2026-04-28 to main on Gitea
- #163 — Refactor 6f0/6f1/guard/guard2 site.pp to use role::*
- #164 — Fix wg_easy::password_hash process-table exposure
- #119 — Vault MinIO root + OIDC client_secret + synapse-media S3 secret
- #146 — Vault Gitea/GitHub PAT in .git/config files
- #305 — Delete /opt/passage/.env.bak.20260507 (overdue past 2026-05-14)

---

## Release-day surfaces (#699 pattern, locked from `release-surfaces.yaml`)

Every v2.2.19 release.sh phase will hit:

1. **Phase 0**: Pre-flight (clean tree, all locales parse, all i18n keys present in all locale files)
2. **Phase 1**: Cross-compile 7 platforms (macOS x2, linux x2, win-gnu, freebsd-x86, netbsd-x86)
3. **Phase 1.5–2.6**: SHA256 manifests + GIT_SHA embed verify
4. **Phase 3**: Git tag v2.2.19 → anvil-source + public
5. **Phase 4**: GitHub Release on culpur/anvil with 29 assets
6. **Phase 4.5**: AnvilHub sha256 manifest → MinIO + pm2 reload
7. **Phase 5**: Homebrew formula bump at culpur/homebrew-anvil
8. **Phase 6**: Anvilhub /about /install /usage + WordPress shortcode (PER-STEP GATED per Arc C1)
9. **Phase 7**: X/Twitter announcement thread (Arc F — auto-fire if `RELEASE_AUTO_POST=1`)
10. **Phase 8**: Release notes draft
11. **Phase 9**: Surface-by-surface HTTP verification (`scripts/verify-release-surfaces.sh`)

Post-pipeline manual:
- WordPress /anvil page body update (page id 619)
- Launch article on culpur.net with DALL-E hero
- Products page (id 394) Anvil card refresh
- Cloudflare cache purge (anvilhub.culpur.net/*, culpur.net/anvil)

---

## Gates before declaring v2.2.19 ready

- [ ] All 8 locales parse + load via rust-i18n (drift gate test)
- [ ] `anvil --lang es` produces Spanish output from cold start
- [ ] Wizard language step renders all 8 native names without mojibake
- [ ] Configure menu language picker persists + switches live
- [ ] Viewer auto-detects browser language on first load
- [ ] Viewer dropdown switches locale on change (reload)
- [ ] /ssh end-to-end from viewer: form → connect → terminal → echo bytes → disconnect (manual smoke + the 6 integration tests pass)
- [ ] release.sh dry-run on a tag candidate completes all 11 phases without silent exits
- [ ] Public README has v2.2.19 What's New + Changelog (in v2.2.17/v2.2.18 prose-quality style)
- [ ] X announcement thread posted via the automated Phase 7 path (not manual)
- [ ] All 5 release-surfaces.yaml invariants pass

---

## Risk register

| Risk | Likelihood | Mitigation |
|------|------------|------------|
| Viewer locale-switch reload loses in-flight session state | Medium | Document the reload trade-off in W4 commit msg; defer live-reactive `t()` to v2.2.20 if it becomes a complaint |
| Wide character languages (zh-CN/ja) overflow TUI columns | Low-medium | Phase A6 test should run on a narrow terminal; if labels overflow, fall back to en for that specific label |
| pt-PT, zh-TW users land in en (not pt-BR, zh-CN) | Low | Acceptable; document the regional fallback choice in W3 commit |
| release.sh Phase 7 (X post) fires before binaries are uploaded | Low | Phase 7 already gates on `RELEASE_AUTO_POST=1` env var (default unset = print-only) |
| Translation drift (someone edits en.yml without updating others) | High | Drift-gate test from Arc A is the structural fix — must run on every PR before merge |

---

## Out-of-scope (explicit deferrals)

- **Tier-2 locales** (ko, it, tr, vi, pl, id, nl, sv, nb, uk) — defer to v2.2.20
- **Tier-3 long tail** (30-50 more) — v2.2.21+ if/when there's translator capacity
- **Live `t()` re-render in viewer** — locale switch will reload the page; in-place re-render is a v2.2.20 polish item
- **#484 Memory Cohesion Arc** — bigger refactor, defer to v2.2.20
- **CC parity v2.1.144+** — defer audit + filing to v2.2.20 cycle unless something P0 surfaces
